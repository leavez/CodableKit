language: swift
osx_image: xcode10
script:
    - xcodebuild -scheme CodableKit -destination 'platform=iOS Simulator,name=iPhone X' build test
after_success:
    - bash <(curl -s https://codecov.io/bash)
